<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../layout/head') %>
</head>

<body>
    <header>
        <%- include('../layout/header') %>
    </header>
    <main>
        <div>
            <%= results.length %> save books
        </div>
        <section>
            <ul>
                <% if ( results.length ) { %>
                    <% results.forEach( item => { %>
                        <li>
                            <div>
                                <h3>
                                    <%= item.title %>
                                </h3>
                                <p>
                                    <%= item.author %>
                                </p>
                                <img src="<%= item.image_url %>">
                                <span><a href="/books/<%= item.id %>" target="_blank">View Details</a></span>
                            </div>
                        </li>
                        <% }); %>
                            <% } %>
            </ul>
        </section>
    </main>
    <footer>
        <%- include('../layout/footer') %>
    </footer>
    <script src="../js/app.js"></script>
</body>

</html>